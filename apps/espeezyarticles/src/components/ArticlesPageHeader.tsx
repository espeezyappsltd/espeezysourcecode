'use client'

import EspeezyAppLogo from '@shared/EspeezyAppLogo'

export default function ArticlesPageHeader() {
  return (
    <header style={{ marginBottom: '1.5rem' }}>
      <EspeezyAppLogo app="articles" variant="hero" />
    </header>
  )
}
