'use client'

import EspeezyAppLogo from '@shared/EspeezyAppLogo'

export default function DevLaunchHeroLogo() {
  return (
    <div className="local-server-hero-logo">
      <EspeezyAppLogo app="devlaunch" variant="hero" />
    </div>
  )
}
